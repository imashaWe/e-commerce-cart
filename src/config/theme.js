import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#FF9494',
        },
        secondary: {
            main: '#fff',
        },
        background: {
            main: "#F7F7F7"
        }
    },
});

export default theme;
